{
  "name": "hipo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "comment": "Thin npm-workspace wrapper that bridges to Deno tasks. All real config lives in deno.json. The src/ paths are also re-exported so Vite in apps/hipo/frontend can import them for the in-page Worker shape (Phase 3). Lives at apps/hipo/.",
  "exports": {
    "./routes": "./src/routes/index.ts",
    "./migrations": "./src/db/migrations.ts",
    "./middleware-session": "./src/middleware/session.ts",
    "./schema": "./src/db/schema.ts"
  },
  "scripts": {
    "dev": "deno task dev",
    "start": "deno task start",
    "check": "deno task check",
    "test": "deno task test",
    "compile:linux": "deno task compile:linux",
    "compile:windows": "deno task compile:windows",
    "clean": "deno task clean"
  }
}
