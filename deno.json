{
  "comment": "Deno workspace root. apps/backend is the entry point; framework packages live under packages/ and are wired into the workspace so their imports resolve regardless of which member runs them.",
  "workspace": [
    "./apps/backend",
    "./packages/sqlite",
    "./packages/server",
    "./packages/auth",
    "./packages/audit",
    "./packages/backup"
  ],
  "lock": true,
  "nodeModulesDir": "auto"
}
