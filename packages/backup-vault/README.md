# @hipo/backup-vault

`BackupTarget` client for the framework's optional vault server
(`@hipo/backup-vault-server`). Stores and retrieves encrypted backup blobs via
HTTP using a personal access token (PAT) for authentication.
