{
  "name": "@hipo/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "comment": "Source-only TypeScript package. No build step — consumers (Vite, Deno) read .ts files directly. Add shared domain logic here as it grows: split algorithm, validators, money helpers, types.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  }
}
